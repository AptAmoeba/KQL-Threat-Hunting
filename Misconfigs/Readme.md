# Misconfigs
This section is intended to store queries that pertain to misconfigurations or dangerous configurations. A prime example would be scanning to identify assets which directly have a Public IP on their machine NIC, meaning a user likely has a misconfigured router/cabling setup causing a public IP to be handed out to their devices in rotation.

